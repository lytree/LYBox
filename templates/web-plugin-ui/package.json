{
  "name": "lybox-web-plugin-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "LYBox Web 插件前端模板：Vite dev server（同源代理联调宿主）→ vite build 产物拷入插件 wwwroot",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build --mode production",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}